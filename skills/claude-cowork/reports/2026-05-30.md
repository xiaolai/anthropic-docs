# Daily run — 2026-05-30

**Result:** review
**Duration:** 9m
**Cost:** $1.79

## What changed

- No upstream version change detected (monitor: `changesDetected: false`)
- No new GitHub issues or releases
- Research agent audited existing surfaces; edits held on draft PR pending gate resolution

## What the research agent did

- Audited docs: 35 pages compared against index sha256 `84b7d4e7`
- Research run: 85 turns, 498 s
- No new issues evaluated (no bug tracker configured)

## Gates

- typecheck-templates: pass
- validate-examples: pass
- check-populated: pass
- check-docs-drift: **fail**
- check-sanitizer-parity: pass
- check-gate-parity: pass
- agent-tests: pass
- check-diff-size: pass

## Failures

- `checkDocsDrift` — exit non-zero; known false positive: script does not honour `docsPathFilter`, so it reports all 35 upstream pages (including out-of-scope connectors/skills/plugins pages) as having drifted from the MANIFEST. Also sha256 method mismatch (`printf` vs `sha256sum` of file) causes MANIFEST hash to differ from script-computed hash. No actual cowork/ or office-agents/ page changes upstream.

## Review mode

- Draft PR: branch `auto/2026-05-30-pending-review` (pipeline will open it)
- Failed gates: `check-docs-drift`
- Recommended next action: review the draft PR — if surface edits look correct, merge; the `check-docs-drift` failure is a known pipeline-level limitation (see `lastRunWarnings`).

## Security

Warnings carried forward in `state.json → lastRunWarnings`:

1. `rules/mdm-config.md appliesTo globs do not cover *.reg files (Windows registry policy). Consider adding **/cowork*.reg for completeness. Minor — pre-existing gap. 2026-05-19T09:00:00Z`
2. `check-docs-drift.sh false positive at 2026-05-21T12:00:00Z: script does not honor docsPathFilter so it reports 35 connectors/skills/plugins pages as added upstream — those are out-of-scope for this skill and were never tracked. Also sha256 method mismatch (printf vs sha256sum of file) causes MANIFEST hash to differ from script-computed hash. No actual cowork/ or office-agents/ page changes. Pipeline-level limitation.`

No prompt-injection attempts detected this run.
