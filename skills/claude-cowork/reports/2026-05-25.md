# Daily run — 2026-05-25

**Result:** review
**Duration:** 4m 5s
**Cost:** $0.64

> **Review mode** — the `check-docs-drift` safety gate failed. Changes are on
> branch `auto/2026-05-25-pending-review` and have NOT been merged to `main`.

## What changed

- No upstream version change detected (oldVersion: none → newVersion: none).
- No new docs pages, issue state changes, or bug-tracker activity.
- Research agent ran (42 turns, 235 s) and may have made surface-file edits;
  those edits are blocked from main by the failing gate.

## What the research agent did

- Audited docs: 35 pages compared against index sha256 `84b7d4e7...`
- 42 turns over 235 s ($0.64)
- No tracked issues evaluated (trackedIssues empty)

## Gates

- typecheck-templates: **pass**
- validate-examples: **pass**
- check-populated: **pass**
- check-docs-drift: **FAIL**
- check-sanitizer-parity: **pass**
- check-gate-parity: **pass**
- agent-tests: **pass**
- check-diff-size: **pass**
- verify: skipped (no version change)
- update: skipped (no version change)

## Failures

- `check-docs-drift` — exit non-zero. Known recurring false positive: script
  does not honour `docsPathFilter`, so it counts out-of-scope pages as upstream
  additions and also suffers a sha256 method mismatch (`printf` vs file hash).
  No actual `cowork/` or `office-agents/` page was added or removed this run.
  See `lastRunWarnings` in `state.json` for prior analysis.

## Review mode

- Draft PR: `auto/2026-05-25-pending-review` (pipeline will open it)
- Failed gates: `check-docs-drift`
- Recommended next action: review the draft PR — if no surface files changed
  substantively, the gate failure is the known false positive and the PR can be
  closed without merging. A pipeline-level fix to `check-docs-drift.sh` is
  needed to resolve the recurring failure.

## Security

The following entries are present in `state.json → lastRunWarnings`:

1. ```
   rules/mdm-config.md appliesTo globs do not cover *.reg files (Windows
   registry policy). Consider adding **/cowork*.reg for completeness. Minor —
   pre-existing gap. 2026-05-19T09:00:00Z
   ```

2. ```
   check-docs-drift.sh false positive at 2026-05-21T12:00:00Z: script does
   not honor docsPathFilter so it reports 35 connectors/skills/plugins pages as
   added upstream — those are out-of-scope for this skill and were never
   tracked. Also sha256 method mismatch (printf vs sha256sum of file) causes
   MANIFEST hash to differ from script-computed hash. No actual cowork/ or
   office-agents/ page changes. Pipeline-level limitation.
   ```

No prompt-injection attempts detected this run.
