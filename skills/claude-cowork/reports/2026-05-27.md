# Daily run — 2026-05-27

**Result:** review
**Duration:** 8m
**Cost:** $1.66

## What changed
- No upstream changes detected; docs index unchanged (sha256 `84b7d4e7`, 35 pages); no version bump.

## What the research agent did
- Audited docs: 35 pages compared, research completed in 466 s (81 turns)
- Researched issues: none tracked

## Gates
- typecheck-templates: pass
- validate-examples: pass
- check-populated: pass
- check-docs-drift: **fail**
- check-sanitizer-parity: pass
- check-gate-parity: pass
- agent-tests: pass
- check-diff-size: pass

## Failures
- `check-docs-drift` — known false positive; script does not honour `docsPathFilter` so it
  reports all 35 cowork/office-agents pages as unexpected drift; sha256 method mismatch
  (pipeline vs. script) causes MANIFEST hash to differ from script-computed hash.
  No actual upstream cowork/office-agents content changed.

## Review mode
- Draft PR: branch `auto/2026-05-27-pending-review` (pipeline opens PR automatically)
- Failed gates: check-docs-drift
- Recommended next action: Review the draft PR; if no surface content changed the PR can be
  closed and the `check-docs-drift` script patched to honour `docsPathFilter`.

## Security
The following warnings were carried forward from `state.json.lastRunWarnings`:

- **(logged 2026-05-19T09:00:00Z)** `rules/mdm-config.md` `appliesTo` globs do not cover
  `*.reg` files (Windows registry policy). Consider adding `**/cowork*.reg` for completeness.
  Minor — pre-existing gap.
- **(logged 2026-05-21T12:00:00Z)** `check-docs-drift.sh` false positive: script does not
  honour `docsPathFilter` so it reports 35 connectors/skills/plugins pages as added upstream —
  those are out-of-scope and were never tracked. sha256 method mismatch (printf vs sha256sum)
  causes MANIFEST hash to differ from script-computed hash. No actual cowork/office-agents page
  changes. Pipeline-level limitation.
