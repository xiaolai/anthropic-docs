# Daily run — 2026-06-02

**Result:** review
**Duration:** ~6m
**Cost:** $0.85

> **Review mode:** The `check-docs-drift` gate failed. Changes are on branch
> `auto/2026-06-02-pending-review` and have NOT been pushed to main.

## What changed

- No upstream version change detected.
- No new docs pages or issue activity.
- Research agent ran and audited existing surface files (55 turns, 338 s, $0.85).

## What the research agent did

- Audited docs: 35 pages compared across `cowork/` and `office-agents/` namespaces.
- Researched issues: 0 evaluated, 0 added, 0 skipped (no tracked repos).

## Gates

- typecheck-templates: pass
- validate-examples: pass
- check-populated: pass
- check-docs-drift: **fail**
- check-sanitizer-parity: pass
- check-gate-parity: pass
- agent-tests: pass
- check-diff-size: pass

## Failures

- `check-docs-drift` — exit code non-zero. Known false positive: script does not
  honour `docsPathFilter`, causing it to report all 35 `connectors/skills/plugins`
  pages as added upstream. The SHA-256 method mismatch (`printf` vs `sha256sum`)
  also causes a MANIFEST hash difference. No actual `cowork/` or `office-agents/`
  page changes were observed. See `lastRunWarnings` entry from 2026-05-21.

## Review mode

- Draft PR: `auto/2026-06-02-pending-review` (pipeline opens PR automatically)
- Failed gates: `check-docs-drift`
- Recommended next action: review the draft PR; if only the known false positive
  triggered, approve and merge manually. The underlying script issue needs a
  pipeline-level fix to honour `docsPathFilter`.

## Security

The following warnings are carried forward from `state.json → lastRunWarnings`:

1. `rules/mdm-config.md` `appliesTo` globs do not cover `*.reg` files (Windows
   registry policy). Consider adding `**/cowork*.reg` for completeness.
   Minor — pre-existing gap. (logged 2026-05-19T09:00:00Z)

2. `check-docs-drift.sh` false positive (logged 2026-05-21T12:00:00Z): script
   does not honour `docsPathFilter` so it reports 35 connectors/skills/plugins
   pages as added upstream — those are out-of-scope for this skill and were never
   tracked. Also SHA-256 method mismatch (`printf` vs `sha256sum` of file) causes
   MANIFEST hash to differ from script-computed hash. No actual `cowork/` or
   `office-agents/` page changes. Pipeline-level limitation.

3. Part C url-check (logged 2026-06-02T00:00:00Z): readiness-check download
   URLs (`claude.ai/api/desktop/.../cowork-readiness-check/latest/redirect`)
   return HTTP 403 for unauthenticated curl — expected, these are authenticated
   download endpoints for signed-in users. URLs are correct as sourced from
   `installation.md`.
