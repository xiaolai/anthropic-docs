# Anthropic Platform — Build with Claude

> **Auto-populated by the daily pipeline.** This file is a stub. After
> the first successful `daily.yml` run for the `anthropic-platform-features`
> skill, it will be rewritten from the upstream docs at
> [platform.claude.com/docs/en/build-with-claude](https://platform.claude.com/docs/en/build-with-claude).

Covers:

- Extended thinking (`thinking` blocks, budget tokens).
- Message batches strategy (cost / throughput tradeoffs).
- Prompt caching strategy (TTL, breakpoints, hit-rate measurement).
- Citations.
- Amazon Bedrock deployment.
- Google Vertex AI deployment.
- Embeddings models.
- Fast mode behavior.
- Context editing (truncation, sliding window).
- Vision (image inputs, document inputs).

## Status

- Stub created during scaffold. Next daily run populates from upstream.
