# Anthropic Platform — Agents & Tools

> **Auto-populated by the daily pipeline.** This file is a stub. After
> the first successful `daily.yml` run for the `anthropic-platform-features`
> skill, it will be rewritten from the upstream docs at
> [platform.claude.com/docs/en/agents-and-tools](https://platform.claude.com/docs/en/agents-and-tools).

Covers:

- **Agent Skills format** — the `.skill` package layout (SKILL.md,
  scripts, references, version, license).
- **MCP connector** — Anthropic's hosted MCP server.
- **Remote MCP servers** — connecting to third-party MCP servers from the API.
- **Tool use** — computer use, code execution, bash tool,
  define-your-own tools (`input_schema`, `tool_choice`).

## Status

- Stub created during scaffold. Next daily run populates from upstream.
