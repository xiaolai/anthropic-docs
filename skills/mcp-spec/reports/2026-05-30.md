# Daily run — 2026-05-30

**Result:** review
**Duration:** 16m
**Cost:** $1.76 (update $0.36 + research $1.33 + mending $0.07)

## What changed
- Python SDK `mcp` (pypi) bumped v1.27.1 → v1.27.2 (published 2026-05-29): `subject` and `claims` added to `AccessToken`; transport sessions now bound to the authenticated principal; experimental Tasks scoped to the session that created them
- TypeScript SDK `@modelcontextprotocol/sdk` v1.29.0 unchanged; spec 2025-11-25 unchanged

## What the research agent did
- Audited docs: 121 pages, index SHA stable; no section updates required
- Researched issues: 0 new bug issues from change-report (newBugIssues empty); no new issues added this run

## Gates
- typecheck-templates: pass
- validate-examples: pass
- check-populated: pass
- check-docs-drift: pass
- check-sanitizer-parity: pass
- check-gate-parity: pass
- agent-tests: pass
- check-diff-size: pass

## Failures
- `mending` (attempt 2): exit code 1 — mending failed to apply changes after 2 runs
- `verify` (attempt 3, 2 mend runs): 3 checks failed
  - `SKILL.md`: "Still contains old v1.29.0"
  - `skills/mcp-spec/README.md`: "Top section still contains old v1.29.0"
  - `GLOBAL`: stale version `1.29.0` found at SKILL.md:30 and README.md:12
  - **Note:** `oldVersion == newVersion == 1.29.0` — only the pypi `mcp` package changed, not the npm SDK. The verify gate's stale-version check appears to have false-positived: the v1.29.0 references in SKILL.md and README.md are correct and current.

## Review mode
- Draft PR: `auto/2026-05-30-pending-review` (pipeline will open it)
- Failed gates: verify
- Recommended next action: Inspect the draft PR — if SKILL.md:30 and README.md:12 correctly show `@modelcontextprotocol/sdk` v1.29.0, the verify gate false-positived (triggered because `mcp` pypi changed but npm SDK did not). The draft can likely be merged or the run re-triggered once the mending/verify logic handles same-version npm cycles correctly.
