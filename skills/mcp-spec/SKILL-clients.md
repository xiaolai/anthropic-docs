# MCP — Clients

> **Auto-populated by the daily pipeline.** This file is a stub. After
> the first successful `daily.yml` run for the `mcp-spec` skill, it
> will be rewritten from the upstream docs at
> [modelcontextprotocol.io](https://modelcontextprotocol.io).

Covers:

- Implementing an MCP client (TypeScript / Python).
- Discovering server capabilities.
- Calling tools — `tools/call`, error handling.
- Reading resources — `resources/read`, subscriptions (`resources/subscribe`).
- Listing prompts — `prompts/list`, `prompts/get`.
- Client-side of sampling — receiving `sampling/createMessage` from a server.
- Roots — exposing filesystem roots to servers.
- Existing MCP clients in the wild (Claude Code, Claude Desktop, others).

## Status

- Stub created during scaffold. Next daily run populates from upstream.
