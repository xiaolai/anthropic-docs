# Daily run — 2026-05-19

**Result:** success
**Duration:** 6m
**Cost:** $1.50 (research agent, 107 turns)

## What changed
- No upstream version bump — `@anthropic-ai/claude-code` remains at v2.1.144
- No docs index hash change detected by monitor
- Research agent found and applied doc-level updates to 4 surface files:
  - SKILL-cli.md: 2 new flags (`--dangerously-load-development-channels`, `--remote-control-session-name-prefix`)
  - SKILL-settings.md: `policyHelper` expanded with `path`, `timeoutMs`, `refreshIntervalMs` sub-fields
  - SKILL-mcp.md: `alwaysLoad` server config field added; `ENABLE_TOOL_SEARCH` value table added
  - SKILL-hooks.md: MCP tool hook fields table added (`server`, `tool`, `input`)
- No issue state changes; no new bug-labeled issues (highest scanned: #60659)

## What the research agent did
- Audited docs: 105 pages compared, no page count change
- Updated: 4 surface files (SKILL-cli.md, SKILL-settings.md, SKILL-mcp.md, SKILL-hooks.md)
- Researched issues: 0 new bug-labeled issues added
- 107 turns, 317s

## Gates
- typecheck-templates: pass
- validate-examples: pass
- check-populated: pass
- check-docs-drift: pass
- check-sanitizer-parity: pass
- check-gate-parity: pass
- agent-tests: pass
- check-diff-size: pass

## Security
`state.json.lastRunWarnings` has 3 entries (all are routine research-agent activity logs,
not prompt-injection attempts):

```
2026-05-19T20:00:00Z: Part A — sandbox section in SKILL-settings.md updated (11 new keys:
allowManagedReadPathsOnly, allowAllUnixSockets, allowLocalBinding, allowMachLookup,
allowManagedDomainsOnly, httpProxyPort, socksProxyPort, enableWeakerNestedSandbox,
enableWeakerNetworkIsolation, bwrapPath, socatPath). Part A — SKILL-hooks.md effort field
added to hook input shape. Part A — rules/mcp.md rule added for mcpServers vs servers key.
Part B — no new bug-labeled issues (lastScannedIssueNumber set to 60427).
Part C — all checks pass.
```

```
2026-05-19T21:00:00Z: Part A — SKILL-slash-commands.md updated: added 14 new commands
(/claude-api, /desktop, /mobile, /passes, /powerup, /privacy-settings, /radio, /remote-env,
/scroll-speed, /setup-bedrock, /setup-vertex, /stickers, /upgrade, /web-setup) and updated
/model description to reflect session-only semantics with d-key default. llms.txt index sha
unchanged. Part B — no bug-labeled issues in anthropics/claude-code. lastScannedIssueNumber
set to 60427. Part C — all checks pass.
```

```
2026-05-19T22:00:00Z: Part A — docs page set unchanged (105 pages). Substantive updates:
SKILL-cli.md added 2 new flags (--dangerously-load-development-channels,
--remote-control-session-name-prefix); SKILL-settings.md expanded policyHelper entry with
path/timeoutMs/refreshIntervalMs sub-fields; SKILL-mcp.md added alwaysLoad server config
field and ENABLE_TOOL_SEARCH value table; SKILL-hooks.md added MCP tool hook fields table
(server, tool, input). Part B — no bug-labeled issues in anthropics/claude-code (highest
issue#60659, no open bugs). Part C — all checks pass: 12/12 schema blocks valid,
5 spot-checked URLs return 200, no duplicate facts.
```

No prompt-injection attempts detected in any entry.

---

> **Note:** This is the sixth pipeline run of 2026-05-19.
> Run 1 — review mode (validate-examples failure; draft PR opened).
> Run 2 — success (added whats-new/2026-w20; 103 → 104 pages).
> Run 3 — success (added prompt-caching.md; 104 → 105 pages).
> Run 4 — success (docs content updated; 11 sandbox keys, hooks effort field, mcp rule).
> Run 5 — success (14 new slash commands documented, /model session-only semantics).
> Run 6 (this run) — success (SKILL-cli.md, SKILL-settings.md, SKILL-mcp.md, SKILL-hooks.md updated).
