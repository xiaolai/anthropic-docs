# Daily run — 2026-05-18

**Result:** review
**Duration:** 22m
**Cost:** $3.29 (update $0.38 + research $2.91; report cost not yet captured in agent-costs.json)

> ⚠️ **Review mode** — two safety gates failed. Changes are on branch
> `auto/2026-05-18-<run-id>-pending-review` (draft PR). Nothing merged to `main`.

## What changed
- Docs index hash changed (`0b1ca29a…` → `1022e166…`); page count unchanged at 103 — in-place content updates, no pages added or removed
- npm version baseline confirmed at `v2.1.143` (first pipeline run against this version; prior state was `0.0.0`)

## What the research agent did
- Audited docs: 103 pages tracked in `knownPages`; index hash drift triggered a full audit pass
- Issues: bug tracker at `lastScannedIssueNumber: 0` — no issues evaluated this run (fresh baseline)
- Research agent ran 109 turns over 19m; update agent ran 31 turns over 2m

## Gates
- typecheck-templates: **pass**
- validate-examples: **fail**
- check-populated: **fail**
- check-docs-drift: **pass**
- check-sanitizer-parity: **pass**
- check-gate-parity: **pass**
- agent-tests: **pass**
- check-diff-size: **pass**

## Failures

### validate-examples
Schema-mapping mismatch: JSON examples in `SKILL-hooks.md` (blocks #2/#3) and `SKILL-plugins.md`
(blocks #2–#5) are being validated against the wrong schemas. The validator maps every JSON block
in `SKILL-hooks.md` to `hook-input.schema.json` (which requires `hook_event_name`) but blocks
#2/#3 are settings-config examples, not hook-input payloads. Similarly, `SKILL-plugins.md`
blocks #2–#5 are `hooks.json`/`monitors.json`/`.lsp.json`/`enabledPlugins` examples, not
`plugin.json` manifests — yet they're validated against `plugin.schema.json`.

Root cause: schema-to-file mapping in `scripts/validate-examples.sh` needs per-block fencing
metadata (e.g. `<!-- schema: settings -->` hints) or the validator needs to skip blocks whose
top-level keys don't match the schema's required fields.

### check-populated
`scaffoldComplete` is `true`, so `check-populated.sh` enforces no residual
`*Populated by the research agent*` stub markers. The gate reported at least one remaining
stub. Likely a SKILL-*.md file the research agent did not fully populate on this run.

## Review mode
- Draft PR: `auto/2026-05-18-<run-id>-pending-review` (pipeline opens it automatically)
- Failed gates: `validate-examples`, `check-populated`
- Recommended next action: review the draft PR; fix schema mapping in `validate-examples.sh`
  or add per-block schema hints, then locate and populate any remaining stub markers, then
  re-run or merge manually

## Security

`state.json.lastRunWarnings` contains the following entries (logged by the pipeline during this run):

```
validate-examples PASS1 FAIL 2026-05-18: SKILL-hooks.md blocks #2/#3 (worked-example
settings.json config checked against hook-input.schema.json which requires hook_event_name —
these are settings config examples, not hook inputs; schema mapping needs update)
```

```
validate-examples PASS1 FAIL 2026-05-18: SKILL-plugins.md blocks #2-#5 checked against
plugin.schema.json which requires name+version — these are hooks.json/monitors.json/
.lsp.json/enabledPlugins examples, not plugin.json manifests; validator maps all
SKILL-plugins.md json blocks to plugin schema
```

These are schema-mapping warnings from `validate-examples`, not prompt-injection attempts.
No injection attempts were detected this run.
