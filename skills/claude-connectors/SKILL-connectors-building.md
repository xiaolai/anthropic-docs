# Claude Connectors — Building Custom Connectors

> **Auto-populated by the daily pipeline.** This file is a stub. After
> the first successful `daily.yml` run for the `claude-connectors`
> skill, it will be rewritten from the upstream docs at
> [claude.com/docs/en/connectors/building](https://claude.com/docs/en/connectors/building).

Covers:

- Building a custom MCP server for the Claude app.
- OAuth setup (registering an OAuth app, redirect URIs, scopes).
- Token handling and refresh.
- Tool / resource exposure model.
- Testing a connector locally and against the Claude app.
- Submission to the connectors directory.

## Status

- Stub created during scaffold. Next daily run populates from upstream.
