# Claude Skills (user-facing)

> **Auto-populated by the daily pipeline.** This file is a stub. After
> the first successful `daily.yml` run for the `claude-connectors`
> skill, it will be rewritten from the upstream docs at
> [claude.com/docs/en/skills](https://claude.com/docs/en/skills).

Covers:

- User-facing Agent Skills (`.skill` packages) in the Claude app.
- Installing / removing skills.
- Browsing the skill directory.
- Skill activation & per-conversation control.

> The deep-spec / authoring side of skills (the `.skill` package
> format itself) lives in the `anthropic-platform-features` skill →
> `SKILL-agents-and-tools.md`. This file covers what users see.

## Status

- Stub created during scaffold. Next daily run populates from upstream.
