# Claude Plugins (user-facing)

> **Auto-populated by the daily pipeline.** This file is a stub. After
> the first successful `daily.yml` run for the `claude-connectors`
> skill, it will be rewritten from the upstream docs at
> [claude.com/docs/en/plugins](https://claude.com/docs/en/plugins).

Covers:

- User-facing plugins in the Claude app.
- Plugin marketplaces.
- Plugin install / scope (user vs project).
- Discovering and managing installed plugins.

> The Claude Code CLI side of plugins lives in `claude-code` →
> `SKILL-plugins.md`. This file covers the Claude app surface.

## Status

- Stub created during scaffold. Next daily run populates from upstream.
