# Daily run — 2026-05-19

**Result:** success
**Duration:** 10m
**Cost:** $2.40

## What changed

- **TypeScript SDK**: v0.3.144 → v0.3.145 (GitHub release, parity with Claude Code v2.1.145)
- Python SDK: v0.2.82 unchanged
- No new bug-labelled issues in anthropics/claude-agent-sdk-typescript (highest issue #327)
- Surface files updated: `SKILL-typescript.md`, `SKILL-python.md`, `rules/claude-agent-sdk-ts.md` —
  subagent invocation tool renamed `Task` → `Agent`; `PostToolUse` hook field `updatedMCPToolOutput`
  deprecated in favour of `updatedToolOutput`; `permissionMode` table updated for `'auto'` (TS-only)

## What the research agent did

- Audited docs: 29 pages compared against MANIFEST SHA256s; multiple sections updated
- Substantive changes processed (across the full day's pipeline runs):
  (1) subagent invocation tool renamed `Task` → `Agent` — both SKILL files and `rules/claude-agent-sdk-ts.md` updated; 'subagents cannot spawn subagents' note added
  (2) `PostToolUse` hook field `updatedMCPToolOutput` deprecated → `updatedToolOutput` (now covers all tool types, not only MCP)
  (3) `permissionMode` options table updated to include `'auto'` (TS-only, model classifier)
  (4) `ttft_ms`, `terminal_reason`, `fast_mode_state` fields added to `SDKResultMessage`; `xhigh` added to `supportedEffortLevels`
  (5) Subagent permission inheritance warning, `stop_reason` common values, `streamable-http` MCP alias, OAuth2 Bearer auth pattern all propagated to SKILL files
- Researched issues: 0 new bug-labelled issues; lastScannedIssueNumber confirmed at 327

## Gates

- typecheck-templates: pass
- validate-examples: pass
- check-populated: pass
- check-docs-drift: pass
- check-sanitizer-parity: pass
- check-gate-parity: pass
- agent-tests: pass
- check-diff-size: pass

## Security

Warnings from `state.json.lastRunWarnings` (7 entries; no prompt-injection attempts detected):

- `2026-05-19T17:24:39Z` — docs_index_changed (hash 065bd6a→ee68ff8e) but no page-level diffs
  provided in change report; no surface-file edits made
- `2026-05-19T17:45:00Z` — Part B skipped; `newBugIssues` empty above lastScannedIssueNumber=0;
  lastScannedIssueNumber updated to 327
- `2026-05-19T19:07:29Z` — docs_index_changed (hash ee68ff8e→c0a299b7); `indexSha256` updated in
  state.json; no surface-file edits made
- `2026-05-19T21:00:00Z` — docs_index_changed (c0a299b7→a24a094d); all 29 pages verified unchanged
  against MANIFEST SHA256s; index change in non-agent-sdk portion of llms.txt; fixed dangling
  `#effortlevel` anchor in SKILL-python.md line 280
- `2026-05-19T22:00:00Z` — SKILL-typescript.md updated with `ttft_ms`, `terminal_reason`,
  `fast_mode_state` on `SDKResultMessage` and `xhigh` on `supportedEffortLevels`; indexSha256
  corrected to a24a094d; bugTracker.lastScannedIssueNumber set to 327
- `2026-05-19T23:30:00Z` — all 29 pages re-fetched (docs infra header injection changed all SHAs);
  5 substantive changes applied: `'auto'` permissionMode, subagent permission inheritance warning,
  `stop_reason` common values, `streamable-http` MCP alias, OAuth2 Bearer auth in MCP section;
  MANIFEST.json updated with fresh SHA256s
- `2026-05-19T00:00:00Z` — all 29 pages re-verified; subagent invocation tool renamed Task→Agent
  across SKILL files and rules; `updatedMCPToolOutput` deprecated → `updatedToolOutput`; permissionMode
  `'auto'` table confirmed; MANIFEST.json refreshed; bugTracker.lastScannedIssueNumber set to 327
