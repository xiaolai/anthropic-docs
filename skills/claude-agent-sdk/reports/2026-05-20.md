# Daily run — 2026-05-20

**Result:** review
**Duration:** 11m
**Cost:** $1.79

## What changed
- No version change — npm `@anthropic-ai/claude-agent-sdk` v0.3.145 and PyPI `claude-agent-sdk` v0.2.82 both unchanged
- No new bug-labeled issues in `anthropics/claude-agent-sdk-typescript` (highest issue #327)
- No new or removed docs pages (29 pages stable)
- Monitor: no changes detected (`changesDetected: false`)

## What the research agent did
- Audited docs: 29 pages re-fetched (SHA256s verified); substantive changes applied
  - SKILL-typescript.md Options: added `planModeInstructions`, `agentProgressSummaries`, `sessionStoreFlush` (alpha) fields
  - SKILL-typescript.md: corrected `outputStyle` (not an `Options` field; must be set via `settings` object); updated `systemPrompt` type with `excludeDynamicSections?: boolean` for prompt-cache reuse
  - SKILL-python.md: added `exclude_dynamic_sections: NotRequired[bool]` to `SystemPromptPreset` TypedDict
  - MANIFEST.json updated with fresh SHA256s for all 29 pages; `state.json` `indexSha256` and `bugTracker.lastScannedIssueNumber` (0 → 327) corrected
- Researched issues: 0 new bug-labeled issues evaluated; bug tracker at issue #327
- 134 research turns in 637s

## Gates
- typecheck-templates: pass
- validate-examples: pass
- check-populated: pass
- check-docs-drift: **fail**
- check-sanitizer-parity: pass
- check-gate-parity: pass
- agent-tests: pass
- check-diff-size: pass
- verify: skipped (no version change)
- update: skipped (no version change)

## Failures
- `check-docs-drift`: failed — research agent applied surface-file changes (new Options fields, TypedDict additions) but the drift gate did not pass the diff; no further error detail in pipeline log

## Review mode
- Draft PR: `auto/2026-05-20-pending-review` (pipeline CI opens it)
- Failed gates: `check-docs-drift`
- Recommended next action: Review the draft PR diff (`planModeInstructions`, `agentProgressSummaries`, `sessionStoreFlush`, `excludeDynamicSections` additions) — if the additions look correct, merge; otherwise let the next run retry

## Security
`state.json.lastRunWarnings` contains 12 entries (all operational — no prompt-injection attempts detected). Entries cover recurring docs-index hash corrections, `bugTracker.lastScannedIssueNumber` corrections (0 → 327), and surface-file updates from prior successful runs (subagent tool rename, `updatedToolOutput` field, `auto` permissionMode, TodoWrite → Task tools migration). Most recent entry (`2026-05-20T12:00:00Z`): 29 pages re-fetched; added `planModeInstructions`, `agentProgressSummaries`, `sessionStoreFlush` (alpha), and `excludeDynamicSections` fields to SKILL-typescript.md and SKILL-python.md; MANIFEST.json updated with fresh SHA256s for all 29 pages.
