# Daily run — 2026-05-20

**Result:** review
**Duration:** 4m
**Cost:** $0.68

> Sixth pipeline run of 2026-05-20 (started 11:27Z). `check-docs-drift` has failed on
> every run today; accumulated surface-file changes are queued on the draft PR branch.

## What changed
- No version change: TS `@anthropic-ai/claude-agent-sdk` v0.3.145, Python `claude-agent-sdk` v0.2.82 — both unchanged
- Monitor: `changesDetected: false` — no npm/PyPI/GitHub release events this cycle
- No new bug-labeled issues (bug tracker at #327, unchanged)
- `update` step skipped (no version bump to apply)

## What the research agent did
- Audited docs: 29 pages monitored, 0 sections updated this run (no upstream changes detected)
- Researched issues: 0 new bug issues (`newBugIssues: []`)
- 46 turns over 207s

## Gates
- typecheck-templates: pass
- validate-examples: pass
- check-populated: pass
- check-docs-drift: **fail**
- check-sanitizer-parity: pass
- check-gate-parity: pass
- agent-tests: pass
- check-diff-size: pass

## Failures
- `check-docs-drift`: failed — surface files contain accumulated edits from earlier runs today
  that differ from main; changes are staged on the draft PR branch, not yet merged

## Review mode
- Draft PR: branch `auto/2026-05-20-pending-review` (pipeline CI opens it)
- Failed gates: `check-docs-drift`
- Recommended next action: Review and merge the draft PR; drift will clear on next run once main is updated

## Security
`state.json.lastRunWarnings` has 15 entries (all operational — no prompt-injection attempts detected):
- Recurring `indexSha256` corrections (`c0a299b7` → `a24a094d`) across multiple runs
- `bugTracker.lastScannedIssueNumber` repeatedly reset 0 → 327 due to stale state.json on each run
- Surface-file update summaries from prior research runs (SDKMessage union expanded 24→30 types,
  new Options fields `toolAliases`/`forwardSubagentText`/`loadTimeoutMs`/`managedSettings`/
  `onElicitation`/`taskBudget`/`planModeInstructions`/`agentProgressSummaries`/`sessionStoreFlush`,
  subagent tool rename Task→Agent, TodoWrite→Task migration, V2 Session API marked removed in v0.3.142,
  hook input fields `duration_ms`/`background_tasks`/`session_crons` documented)
- No external content attempted to issue instructions; no security action taken
