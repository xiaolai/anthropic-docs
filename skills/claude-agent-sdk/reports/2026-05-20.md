# Daily run — 2026-05-20

**Result:** review
**Duration:** 9m
**Cost:** $2.61

## What changed
- No version change — npm `@anthropic-ai/claude-agent-sdk` v0.3.145 and PyPI `claude-agent-sdk` v0.2.82 both unchanged
- No new bug-labeled issues in `anthropics/claude-agent-sdk-typescript` (highest issue #327)
- No new or removed docs pages (29 pages stable)
- Monitor: no changes detected (`changesDetected: false`)

## What the research agent did
- Audited docs: 29 pages re-fetched; substantive changes applied to SKILL-typescript.md:
  - New `Options` fields: `toolAliases`, `forwardSubagentText`, `loadTimeoutMs` (alpha), `managedSettings` / `onElicitation` / `taskBudget` (Advanced)
  - `SDKMessage` union expanded 24 → 30 types: added `SDKSessionStateChangedMessage`, `SDKNotificationMessage`, `SDKMemoryRecallMessage`, `SDKElicitationCompleteMessage`, `SDKAPIRetryMessage`, `SDKMirrorErrorMessage`
  - V2 Session API section marked as removed in v0.3.142
  - Hook Input Fields table: added `duration_ms` (PostToolUse/PostToolUseFailure), `background_tasks` / `session_crons` (Stop/SubagentStop)
  - MANIFEST.json regenerated with fresh SHA256s for all 29 pages
- Researched issues: 0 new bug-labeled issues; bug tracker at #327
- 107 research turns in 523s

## Gates
- typecheck-templates: pass
- validate-examples: pass
- check-populated: pass
- check-docs-drift: **fail**
- check-sanitizer-parity: pass
- check-gate-parity: pass
- agent-tests: pass
- check-diff-size: pass
- verify: skipped (no version change)
- update: skipped (no version change)

## Failures
- `check-docs-drift`: failed — research agent applied surface-file changes but drift gate did not clear; no further error detail in pipeline log

## Review mode
- Draft PR: `auto/2026-05-20-pending-review` (pipeline CI opens it)
- Failed gates: `check-docs-drift`
- Recommended next action: Review draft PR diff (`toolAliases`, `forwardSubagentText`, SDKMessage union 24→30 types, V2 Session removed, hook fields) — if correct, merge; otherwise let next run retry

## Security
`state.json.lastRunWarnings` contains 13+ entries (all operational — no prompt-injection attempts detected). Entries cover recurring docs-index hash corrections, `bugTracker.lastScannedIssueNumber` corrections (0 → 327), and surface-file updates across multiple runs today. Most recent entry: 29 pages re-fetched; `toolAliases`, `forwardSubagentText`, `loadTimeoutMs`, `managedSettings`/`onElicitation`/`taskBudget` added to SKILL-typescript.md Options; `SDKMessage` union expanded 24→30 types; V2 Session API marked removed in v0.3.142; hook input fields updated; MANIFEST.json regenerated.
