# Daily run — 2026-06-02

**Result:** review
**Duration:** 18m
**Cost:** $2.31

> ⚠️ **Review mode** — draft PR opened on branch `auto/2026-06-02-pending-review`.
> The `check-diff-size` safety gate failed; changes are NOT on `main`.

## What changed

- **npm version bump**: `@anthropic-ai/claude-agent-sdk` 0.3.159 → 0.3.160
- **GitHub release** `v0.3.160` (published 2026-06-02T02:10:21Z):

  ```
  Fixed SDK hook callbacks swallowing abort signals: aborting during a
  PostToolUse hook now ends the turn with a final `result` message instead
  of hanging the calling process
  ```

- **Docs index hash changed**: 6e6b703a → cb0731c5 (30 pages; no pages added or removed)
- **Python SDK** `claude-agent-sdk` v0.2.87 — unchanged

## What the research agent did

- Audited docs: 30 pages compared, sections updated to reflect v0.3.160 hook abort-signal behaviour
- Researched issues: 6 evaluated (#336–#341); 3 added as known issues, 3 skipped
  - **Added KI 1** (#337): Bedrock regression v0.3.152+ — empty thinking blocks emitted in tool-use turns
  - **Added KI 2** (#338): `interrupt()` emits truncated assistant message indistinguishable from normal completion
  - **Added KI 3** (#339): `query()` iterator closes without `SDKResultMessage` when Agent tool + `end_turn` used
  - **Skipped** #336 (enhancement request), #340 (docs issue), #341 (community example PR)
- Mending: 1 initial attempt failed (exit 1); verify succeeded on attempt 3 after 2 mending runs

## Gates

| Gate | Result |
|---|---|
| typecheck-templates | ✅ pass |
| validate-examples | ✅ pass |
| check-populated | ✅ pass |
| check-docs-drift | ✅ pass |
| check-sanitizer-parity | ✅ pass |
| check-gate-parity | ✅ pass |
| agent-tests | ✅ pass |
| **check-diff-size** | ❌ **fail** |
| verify | ✅ pass (27/27 checks) |

## Failures

- **mending** (attempt 1): exit code 1 — initial mending run failed; pipeline retried (2 mend runs total before verify passed)
- **check-diff-size**: exit code 1 — diff size exceeded threshold; exact bound not reported in pipeline log

## Review mode

- Draft PR: `auto/2026-06-02-pending-review` (pipeline will open it)
- Failed gates: `check-diff-size`
- Recommended next action: review the draft PR — the diff may be legitimately large (3 new known issues + hook abort-signal docs); approve and merge manually if content is correct

## Security

`state.json.lastRunWarnings` contains **13+ entries** from prior runs (2026-05-19 through 2026-05-20). All are operational pipeline notes (docs-index hash refreshes, issue-scanner corrections, surface-file update summaries). No prompt-injection attempts were recorded. No new warnings were appended this run.
