# Daily run — 2026-05-18

**Result:** review
**Duration:** 5m
**Cost:** $0.88

> ⚠️ **Review mode** — changes are on branch `auto/2026-05-18-pending-review` and have NOT been pushed to `main`. The `check-diff-size` gate failed: the research agent's first full baseline audit produced a diff larger than the permitted threshold.

## What changed

- No upstream version change detected (npm/pypi/GitHub all at `none`).
- No new GitHub issues or issue-state changes in the change report.
- Research agent performed first full baseline audit of all surface files; resulting diff triggered the `check-diff-size` gate.
- 2 new known issues added to SKILL-known-issues.md (KI 1031, KI 1038).

## What the research agent did

- Ran 78 turns over 309 s; no prior audited version, so a full baseline audit was performed.
- Evaluated 5 open GitHub issues: 2 added as known issues, 3 skipped.
  - **Added KI 1031**: `claude-opus-4-7` emits a literal `<<<SENTINEL\n` prefix on every string value inside a single tool's input JSON when called via Bedrock.
  - **Added KI 1038**: Vertex AI rejects document URL sources with a misleading error path; recommended workaround is to use base64-encoded content instead.
  - **Skipped #1028**: supply-chain / security concern, not a user-facing API bug.
  - **Skipped #1035**: issue closed (zodOutputFormat / zod3 incompatibility was fixed upstream).
  - **Skipped #1036**: feature request (TracingChannel observability), not a bug.

## Gates

| Gate | Result |
|---|---|
| typecheck-templates | pass |
| validate-examples | pass |
| check-populated | pass |
| check-docs-drift | pass |
| check-sanitizer-parity | pass |
| check-gate-parity | pass |
| agent-tests | pass |
| check-diff-size | **fail** |

## Failures

- **check-diff-size** — diff from first baseline audit exceeded the allowed size threshold. No granular exit-code detail is available in the pipeline log for this gate.

## Review mode

- Draft PR: `auto/2026-05-18-pending-review` *(pipeline will open it)*
- Failed gates: `check-diff-size`
- Recommended next action: Review the draft PR to confirm the baseline surface-file content is accurate, then merge. Subsequent runs will produce incremental diffs that are likely to pass the size gate.
