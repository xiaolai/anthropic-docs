# Anthropic Beta API — Reference

> **Auto-populated by the daily pipeline.** This file is a stub. After
> the first successful `daily.yml` run for the `anthropic-api` skill,
> it will be rewritten from the upstream docs at
> [platform.claude.com/docs/en/api/beta](https://platform.claude.com/docs/en/api/beta).

Covers:

- Features gated behind the `anthropic-beta` header.
- Beta-only endpoints.
- Opt-in feature flags and their semantics.
- Graduation status — which betas have left beta, which were retired.

## Status

- Stub created during scaffold. Next daily run populates from upstream.
